import { Routes } from '@angular/router';
import { routes } from './app.routes';

export const appConfig = {
  providers: [], // Aquí puedes agregar tus proveedores si los necesitas
  routes: routes // Asegúrate de que esto esté correctamente definido
};
